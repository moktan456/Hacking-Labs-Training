# Lab 2 — CTF Challenge

**No hints below.**

## Objective

Two flags, hidden using OSINT/recon techniques only — no exploitation involved:

- `user.txt`-equivalent: hidden in a DNS record on `10.10.2.5`
- `root.txt`-equivalent: hidden behind a clue on the web server at `10.10.2.10`

## Rules

- Targets: `10.10.2.0/24` only
- Attack from: `lab2-attacker`
- Flag format: `flag{...}`

Good luck.
