# Lab 6 — CTF Challenge

**No hints below.**

## Objective

Two flags on `lab6-ssh` (10.10.6.10), reachable via password attacks against two different accounts:

- `user.txt`
- `root.txt`

Bonus: the web login form on `lab6-web` (10.10.6.11) returns its own flag once you find valid credentials.

## Rules

- Targets: `10.10.6.0/24` only
- Attack from: `lab6-attacker`
- Flag format: `flag{...}`

Good luck.
