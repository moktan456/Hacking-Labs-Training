# Lab 5 — CTF Challenge

**No hints below.**

## Objective

Two flags on `lab5-smb` (10.10.5.12):

- `user.txt` — on the public share
- `root.txt` — on the private share (requires valid credentials — check what LDAP enumeration revealed)

Bonus: a third flag is sitting in a MySQL table on `lab5-mysql` (10.10.5.11).

## Rules

- Targets: `10.10.5.0/24` only
- Attack from: `lab5-attacker`
- Flag format: `flag{...}`

Good luck.
